
namespace RabbitMq.Connector.Model
{
    public enum BasicPorperties
    {
        content_encoding,
        correlation_id,
        content_type,
        message_id,
        cluster_id,
        expiration,
        timestamp,
        priority,
        reply_to,
        user_id,
        app_id,
        type,
    }
}